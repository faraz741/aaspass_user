import { FilterCategoriesPipePipe } from './filter-categories-pipe.pipe';

describe('FilterCategoriesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoriesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
